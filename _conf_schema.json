{
  "enable": {
    "description": "是否启用插件",
    "type": "bool",
    "default": true
  },
  "whitelist": {
    "description": "群聊白名单（群号列表），只有白名单内的群才会发送入群欢迎",
    "type": "list",
    "default": [],
    "hint": "填写需要生效的群号，例如 [\"123456789\", \"987654321\"]"
  },
  "welcome_config": {
    "description": "入群欢迎配置",
    "type": "object",
    "items": {
      "send_card": {
        "description": "是否发送群聊卡片",
        "type": "bool",
        "default": true
      },
      "card_group_id": {
        "description": "群聊卡片推荐的目标群号",
        "type": "string",
        "default": "",
        "hint": "QQ 群号，留空则使用当前群号"
      },
      "send_forward": {
        "description": "是否发送聊天记录（合并转发消息）",
        "type": "bool",
        "default": true
      },
      "forward_nodes": {
        "description": "(旧版兼容) 合并转发消息节点",
        "type": "list",
        "default": [],
        "hint": "已由 raw_messages 替代，此项保留用于旧数据兼容"
      },
      "raw_messages": {
        "description": "合并转发原始数据（由 /welcome set_forward 自动填写，请勿手动编辑）",
        "type": "list",
        "default": [],
        "hint": "存储 get_forward_msg API 返回的原始 messages 数组，发送时自动转换为标准格式"
      },
      "send_image": {
        "description": "是否发送图片",
        "type": "bool",
        "default": true
      },
      "image_url": {
        "description": "图片 URL 或本地文件路径",
        "type": "string",
        "default": "",
        "hint": "支持 http/https URL 或本地绝对路径，如 C:/images/welcome.png"
      }
    }
  }
}
